const addOne = (n: number): number => n + 1;
const addTwo = (n: number): number => n + 2;

// TODO: Make an addThree function by composing the above functions

const addThree = (n: number): number => {
    return 0
};

console.log(addThree(39) + " should = 42");
